# Auto-provisions Prometheus as Grafana's data source so the
# dashboard works on first boot with zero clicking around.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
